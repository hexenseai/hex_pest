# Kale İlaçlama - Ortam değişkenleri
# Bu dosyayı .env olarak kopyalayıp değerleri doldurun.
# cp .env.example .env

# Zorunlu - Django gizli anahtar (production'da mutlaka değiştirin)
SECRET_KEY=django-insecure-9he57i4o*=_jhvcr^l(l$*@%5)g!g$52uds36=n6^55&&@6+8=

# Geliştirme: True, Production: False
DEBUG=True

# Virgülle ayrılmış host listesi (ör: 127.0.0.1,localhost,.example.com)
ALLOWED_HOSTS=127.0.0.1,localhost

# Veritabanı
# Boş bırakılırsa SQLite (db.sqlite3) kullanılır.
# PostgreSQL: postgres://user:password@host:port/dbname
# MySQL: mysql://user:password@host:port/dbname
DATABASE_URL=

# Dosya yolları (opsiyonel - varsayılan: proje kökünde media/, staticfiles/)
# MEDIA_ROOT=media
# STATIC_ROOT=staticfiles
